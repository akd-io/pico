{
  "name": "pico",
  "private": true,
  "type": "module",
  "scripts": {
    "download": "bun run versions/download-versions.ts",
    "unzip": "bun run versions/unzip.ts",
    "print-envs": "bun run versions/print-envs.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
